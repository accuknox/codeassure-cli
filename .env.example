# API keys — copy to .env and fill in
# The env var prefix must match the provider in codeassure.json (OPENAI, ANTHROPIC, GEMINI, ...)

# Local llama.cpp / OpenAI-compatible
OPENAI_API_KEY=not-needed

# Claude
# ANTHROPIC_API_KEY=sk-ant-...

# Gemini
# GEMINI_API_KEY=...
